# Experiment-run boundary

Local experiment outputs, logs, manifests, checkpoints, and per-utterance predictions belong in ignored storage.

Commit only privacy-safe aggregate evidence and reproducible configurations.

Every accepted run must be reconstructable from hashes, revisions, commands, and external artifact identifiers.
