# Configurations

This directory will contain versioned, reviewable experiment and inference configurations.

Planned groups:

```text
inference/
training/
active-cycle/
evaluation/
release/
```

Configuration files must not contain secrets, private paths, or unpublished dataset locations.

Every released experiment records a configuration hash.
