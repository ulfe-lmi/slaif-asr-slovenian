# Tests

The scaffold contains no executable test suite yet.

See [`docs/testing-strategy.md`](../docs/testing-strategy.md).

The first implementation PR should introduce repository-hygiene checks and focused tests for its own tooling. GPU tests must be explicitly tagged and must not be represented as passed when they were skipped.
