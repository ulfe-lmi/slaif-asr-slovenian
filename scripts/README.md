# Scripts

Executable tooling is intentionally absent from the strategic scaffold.

The first implementation work order will add:

- official checkpoint download;
- runtime contract inspection;
- tokenizer audit;
- cache-aware streaming inference wrappers.

Scripts must be small, typed where practical, testable, and free of embedded private paths or credentials.
